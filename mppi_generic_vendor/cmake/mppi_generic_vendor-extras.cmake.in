# Generated by mppi_generic_vendor. Consumers may use this to select their
# CUDA backend without probing the filesystem or silently changing versions.
set(mppi_generic_vendor_VERSION "@MPPI_GENERIC_VERSION@")
set(mppi_generic_vendor_GIT_TAG "@MPPI_GENERIC_GIT_TAG@")
set(mppi_generic_vendor_CUDA_BUILT "@MPPI_GENERIC_VENDOR_BUILT@")

if(mppi_generic_vendor_CUDA_BUILT)
  find_package(MPPI-Generic 0.9 REQUIRED CONFIG)
endif()
